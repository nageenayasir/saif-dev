<style>
  #id-{{ section.id }}{
  	background-color:{{ section.settings.section_bg_color }};
  }
</style>
<section id="id-{{section.id}}" class="wholesale-icons-section">
  <div class="container">
    <div class="row">
      {% for block in section.blocks %}
        <div class="col-sm-12 col-md-3 col-lg-3 wholesale-icons-columns">
          <div class="wholesale-icons-wrap" data-aos="fade-up">
            {% if block.settings.the_logo != blank %}<img loading="lazy"  src="{{ block.settings.the_logo | image_url: width: 2000 }}">{% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
{% schema %}
  {
    "name": "Wholesale Icons",
    "settings": [
      {
            "type": "header",
            "content": "Content Style"
         },
  	     {
           "type": "color",
           "id": "section_bg_color",
           "label": "Section background color"
        }
    ],
	"blocks": [
     {
      "type": "logo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "the_logo",
          "label": "Logo"
        }
      ]
     }
	],
    "presets": [{
      "name": "Wholesale Icons"
    }]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}